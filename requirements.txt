# ============================================================
# Smart Lost & Found — Runtime + development dependencies
# All versions are pinned for reproducible builds.
# ============================================================

# --- Web framework ---
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9         # required for FastAPI file uploads

# --- Data validation & settings ---
pydantic==2.6.4
pydantic-settings==2.2.1
python-dotenv==1.0.1

# --- AI provider SDKs ---
openai==1.30.5                  # OpenAI GPT-4o (primary VLM + embeddings)
google-genai==1.10.0            # Google Gemini 2.0 Flash (secondary/failover VLM)
anthropic==0.26.1               # Anthropic Claude (optional third provider)

# --- Numerical / AI ---
numpy==1.26.4

# --- Resilience ---
tenacity==8.2.3                 # retry decorator for ai.* calls

# --- Async storage ---
aiosqlite==0.20.0               # async SQLite (dev + tests)
asyncpg==0.29.0                 # async PostgreSQL (production)

# --- HTTP client ---
httpx==0.27.0                   # async HTTP + FastAPI TestClient

# --- Testing ---
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# --- Code quality ---
ruff==0.3.4
mypy==1.9.0

# --- Observability (bonus: OpenTelemetry tracing) ---
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp==1.24.0

# --- Web UI (bonus: Streamlit) ---
streamlit==1.32.2
